@model IEnumerable<TSheet.Data.AssignedRole>

@{
    Layout = null;
}
<link href="~/Content/css/sb-admin-2.min.css" rel="stylesheet" />
<link href="~/Content/css/sb-admin-2.css" rel="stylesheet" />
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Totaladmin</title>
</head>
<body>

    <table class="table">
        <tr>
            <th>
                @* @Html.DisplayNameFor(model => model.Registration.FirstName)*@
                Name
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Role.RoleName)
            </th>

        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Registration.FirstName)
                    @Html.DisplayFor(modelItem => item.Registration.LastName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Role.RoleName)
                </td>


            </tr>
        }

    </table>
</body>
</html>
